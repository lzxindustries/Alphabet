execute_process(COMMAND "${MACDEPLOYQT_EXECUTABLE}" ${CMAKE_BINARY_DIR}/${PROJECT_NAME}-${PROJECT_VERSION}-Mac.app -dmg
                 WORKING_DIRECTORY ${CPACK_PACKAGE_DIRECTORY}
                )
